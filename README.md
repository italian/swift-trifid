# 🐍 Snake AI - Reinforcement Learning Project

Проект по обучению нейросети играть в "Змейку" с помощью Deep Q-Learning.

## 🎮 Доступные режимы

### 1. Обучение AI
Запустите обучение нейросети с нуля или продолжите существующую модель:

```bash
# Начать обучение с нуля
python agent.py

# Продолжить обучение существующей модели
python agent.py --load model/model_20231128-133000.pth

# Изменить терпение Early Stopping (по умолчанию 50)
python agent.py --patience 100

# Изменить скорость обучения (по умолчанию 40)
python agent.py --speed 100
```

**Параметры:**
- `--load` - путь к существующей модели для продолжения обучения
- `--new` - начать обучение с нуля (по умолчанию)
- `--patience` - сколько игр ждать без улучшения рекорда перед остановкой
- `--speed` - скорость игры во время обучения

### 2. Игра человеком
Сыграйте сами и посмотрите, сможете ли обойти AI:

```bash
python play_human.py
```

**Управление:**
- `↑ / W` - вверх
- `↓ / S` - вниз
- `← / A` - влево
- `→ / D` - вправо
- `SPACE` - пауза/возобновить
- `R` - начать заново

### 3. Демонстрация обученной модели
Посмотрите, как играет обученная нейросеть:

```bash
# Запустить с лучшей моделью (по умолчанию model/model.pth)
python play_ai.py

# Выбрать конкретную модель
python play_ai.py --model model/model_20231128-133000.pth

# Изменить скорость игры (больше = быстрее)
python play_ai.py --speed 60

# Сыграть 5 игр и показать статистику
python play_ai.py --games 5
```

**Параметры:**
- `--model` - путь к модели (по умолчанию `model/model.pth`)
- `--speed` - скорость игры в FPS (по умолчанию 20)
- `--games` - количество игр для демонстрации (0 = бесконечно)

### 4. Таблица лидеров
Посмотрите лучшие результаты моделей и людей:

```bash
python leaderboard.py
```

## 📊 Результаты обучения

Во время обучения отображается график в реальном времени:
- **Синяя линия** - очки за каждую игру
- **Оранжевая линия** - средний счет (тренд обучения)

## 🧠 Как это работает?

Подробное объяснение алгоритма смотрите в [code_explanation.md](code_explanation.md)

## 📁 Структура проекта

```
swift-trifid/
├── game.py          # Игровая среда (Pygame)
├── model.py         # Нейросеть (DQN)
├── agent.py         # RL Агент + обучение
├── helper.py        # Визуализация графиков
├── play_human.py    # Режим игры для человека
├── play_ai.py       # Демонстрация AI
├── audit.py         # Проверка качества кода
├── leaderboard.py   # Таблица лидеров
├── task.md          # Список задач
├── walkthrough.md   # Описание последних изменений
└── model/           # Сохраненные модели
```

## 🛠️ Зависимости

```bash
pip install pygame torch torchvision matplotlib ipython bandit pylint mypy
```

## 🏆 Человек против AI

**Challenge:** Сыграйте несколько раз в `play_human.py`, запомните свой лучший результат.
Затем запустите `play_ai.py --games 10` и сравните!

Кто круче? 😎
